services:
  - type: web
    name: minimarket
    env: node
    region: oregon
    buildCommand: 'npm install'
    startCommand: 'npm start'
    envVars:
      - key: NODE_ENV
        value: production
